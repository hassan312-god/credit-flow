# Copiez en .env et renseignez (ou sur Vercel : Settings → Environment Variables)
# Pour le build Tauri (desktop) : soit .env présent lors de `tauri build`, soit copiez en public/supabase-config.json (voir public/supabase-config.json.example)
NUXT_PUBLIC_SUPABASE_URL=
NUXT_PUBLIC_SUPABASE_ANON_KEY=
